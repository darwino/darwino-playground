# Read one Domino view entry
{
	# One simple string key
	e1: DominoViewEntry(database:"DarwinoReports.nsf", name:"4. Products", key:"S10_1949") {
		_unid,
		_noteId,
		_position,
		_read,
		_siblingCount,
		_descendantCount,
		_childCount,
		_indentLevel,
		_isCategory,
    	code: string(path:"PRODUCTCODE"),
    	name: string(path:"PRODUCTNAME"),
	}
	# A simple key as json (keys)
	e2: DominoViewEntry(database:"DarwinoReports.nsf", name:"4. Products", keys:"'S10_1949'") {
		_unid,
		_noteId,
		_position,
		_read,
		_siblingCount,
		_descendantCount,
		_childCount,
		_indentLevel,
		_isCategory,
    	code: string(path:"PRODUCTCODE"),
    	name: string(path:"PRODUCTNAME"),
	}
	# A simple key as a json array (keys) - This can also be used for multiple value keys
	e3: DominoViewEntry(database:"DarwinoReports.nsf", name:"4. Products", keys:"['S10_1949']") {
		_unid,
		_noteId,
		_position,
		_read,
		_siblingCount,
		_descendantCount,
		_childCount,
		_indentLevel,
		_isCategory,
    	code: string(path:"PRODUCTCODE"),
    	name: string(path:"PRODUCTNAME"),
	}
}
