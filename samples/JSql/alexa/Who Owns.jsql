-- params={model:'Doctor Who'}

SELECT O.$.fullName owner, 
	   P.$.name name
	FROM owners O
		LEFT OUTER JOIN owns R ON R.$.owner=O._unid
		LEFT OUTER JOIN pinball P ON R.$.ipdb=P._unid
	WHERE P.$.name=:model -- 'Doctor Who'
	ORDER BY owner